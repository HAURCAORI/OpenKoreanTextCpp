#pragma once
#include <map>
#include "KoreanPos.hpp"

namespace OpenKorean {
class KoreanTokenizer {
private:
    static const int TOP_N_PER_STATE = 5;
    static const int MAX_TRACE_BACK = 8;
    //static const std::map<KoreanPos>;
public:

};
}